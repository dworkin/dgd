NAME
	save_object - save variables of an object

SYNOPSIS
	void save_object(string file)


DESCRIPTION
	Save all global variables in an object that are not private, static
	or inherited through labelled inheritance, to a file.  Only non-zero
	and non-object values are actually saved.

ERRORS
	An error will result if the variables could not be saved.

SEE ALSO
	kfun/save_object
