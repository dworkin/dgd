NAME
	decrypt - decrypt a string

SYNOPSIS
	string decrypt(string cipher, string mesg, string key...)


DESCRIPTION
	For any given cipher, decrypt("CIPHER key", key1, key2, ...) returns a
	key prepared for decryption, and
	decrypt("CIPHER", str, prepared_key, extra_key1, extra_key2, ...) 
	decrypts a string.  For an unknown cipher, nil is returned.
	The only currently defined cipher is DES.

SEE ALSO
	kfun/crypt, kfun/decrypt
