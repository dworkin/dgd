NAME
	create - initialize object

SYNOPSIS
	void create()
	void create(int clone)


DESCRIPTION
	This function is called when an object is initialized.  If the
	object is not clonable, the first form is used.  If it is
	clonable, the second form is used, the argument being 1 if the
	object is a clone, or 0 if it is a master object.
